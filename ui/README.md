# ui/

React + Vite + TypeScript + Tailwind "Investigation Console" — consumes the
FastAPI SSE stream to show live agent progress, findings, and the final
verdict + kill-chain timeline.

Implemented in Feature 8.
